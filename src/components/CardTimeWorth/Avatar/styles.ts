import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    justifyContent: 'center',
    maxWidth: 60,
  },

  textTitle: {
    textAlign: 'center',
  },

  textValue: {
    fontWeight: 'bold',
    textAlign: 'center',
  },
});
